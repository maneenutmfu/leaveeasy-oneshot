# รายงานผลการทดสอบ LeaveEasy

- **วันที่ทดสอบ:** 2026-09-20
- **URL ที่ทดสอบ:** https://leaveeasy-oneshot-82d59.web.app
- **เครื่องมือ:** Playwright MCP (เบราว์เซอร์จริง, ไม่ใช่ local dev server)
- **บัญชีที่ใช้:**
  - leaveeasy.tester.employee@example.com (employee)
  - leaveeasy.tester.manager@example.com (manager)
  - leaveeasy.tester.hr@example.com (hr)
  - leaveeasy.tester.employee2@example.com (employee คนที่สอง — สมัครเพิ่มเพื่อทดสอบเทสต์ความปลอดภัยข้ามบัญชี)
- **ข้อมูลทดสอบที่สร้างขึ้นระหว่างทดสอบ** (อยู่ภายใต้บัญชี tester employee ไม่กระทบข้อมูล seed u001-u003 / lr001-lr005):
  - "ทดสอบระบบโดย QA Tester (US-02)" → ถูกอนุมัติระหว่างทดสอบ US-04
  - "ทดสอบไม่อนุมัติ (US-04/05) โดย QA" → ถูกไม่อนุมัติระหว่างทดสอบ US-04/05
  - "ทดสอบหลัง AI ล้มเหลว ยังบันทึกได้" → เหลือสถานะรอพิจารณา
  - "ทดสอบลบใบลา (US-07) โดย QA" → สร้างแล้วลบสำเร็จระหว่างทดสอบ US-07 (ไม่เหลืออยู่ในระบบ)
  - ประเภทการลาชั่วคราว "ลาทดสอบ QA" / "ลาทดสอบ QA (แก้ไขแล้ว)" → เพิ่มและลบแล้วระหว่างทดสอบ US-06 (ไม่เหลืออยู่ในระบบ)

สรุปรวม: **ผ่านครบ 9 ข้อ (US-01 ถึง US-09)** — US-09 มีการรายงานผิดพลาดในรอบทดสอบแรก แต่ตรวจซ้ำแล้วยืนยันว่าผ่าน (ดูหัวข้อ "แก้ไขผลการทดสอบ")

---

## US-01 — ดูรายการใบลาทั้งหมด

**ผลการทดสอบ: ผ่าน**

- ตารางมีคอลัมน์ครบ: หัวข้อ · ประเภทการลา · สถานะ · ผู้ขอลา · วันที่ลา
- ชื่อผู้ขอลาแสดงเป็นภาษาไทย (เช่น "สมชาย ใจดี", "ทดสอบ ผู้ขอลา") ไม่ใช่รหัส เช่น u001
- ป้ายสถานะสีถูกต้องครบทั้ง 3 แบบ (ตรวจด้วยภาพหน้าจอจริง): รอพิจารณา = เหลือง, อนุมัติ = เขียว, ไม่อนุมัติ = แดง
- กดแถวไปหน้ารายละเอียดได้ถูกต้อง (URL เปลี่ยนเป็น `leave-request-detail.html?id=<docId>`)
- บัญชี employee ใหม่ที่ยังไม่มีใบลาเป็นของตัวเอง แสดงข้อความ "ยังไม่มีใบขอลาในระบบ" ถูกต้อง (ไม่ใช่ตารางว่าง)

---

## US-02 — ยื่นใบขอลาใหม่

**ผลการทดสอบ: ผ่าน**

- ฟอร์มมีช่องครบ: หัวข้อ, เหตุผลการลา (textarea), ประเภทการลา (dropdown), วันที่เริ่มลา, วันที่สิ้นสุด
- ประเภทการลาใน dropdown โหลดจาก Firestore จริง (แสดง "กำลังโหลดประเภทการลา..." ระหว่างรอ แล้วเปลี่ยนเป็นรายการจริง 3 ประเภทจาก seed: ลาพักร้อน/ลาป่วย/ลากิจ) — ยืนยันเพิ่มเติมว่าเป็นข้อมูลจริงโดยเพิ่ม/ลบประเภทใหม่ผ่าน HR แล้วเห็นการเปลี่ยนแปลงทันทีในหน้านี้ (ดู US-06)
- กดบันทึกแล้วมีใบลาใหม่เกิดขึ้นจริงใน Firestore (ตรวจสอบด้วยการรีเฟรชหน้ารายการ และเปิดใหม่ด้วยบัญชี manager) พร้อม redirect กลับหน้ารายการอัตโนมัติ
- ใบใหม่มีสถานะเริ่มต้น "รอพิจารณา" และมีวันเวลาที่ยื่น (`วันที่ยื่น`) ติดมาอัตโนมัติ
- ปุ่มยกเลิกพากลับหน้ารายการโดยไม่บันทึกจริง (ทดสอบโดยกรอกหัวข้อไว้แล้วกดยกเลิก แล้วตรวจว่าหน้ารายการยังว่างเปล่าเหมือนเดิม)
- **เพิ่มเติม (ทดสอบภายหลัง 2026-09-20):** กรอกฟอร์มครบทุกช่องยกเว้น "หัวข้อ" (เว้นว่างไว้) แล้วกดบันทึก → ระบบไม่บันทึกข้อมูลจริง (ยืนยันด้วยการเปิดหน้ารายการหลังจากนั้น ไม่มีใบลาใหม่เกิดขึ้น) และมีข้อความแจ้งเตือนขึ้นจริง คือ tooltip ของเบราว์เซอร์ "Please fill out this field." ที่โฟกัสไปยังช่องหัวข้อทันที เพราะช่อง `<input id="ช่องหัวข้อ">` มี attribute `required` (ดู `new-leave-request.html` บรรทัด 18) ทำให้เบราว์เซอร์เช็กและกันการ submit ไว้ตั้งแต่ก่อนโค้ด JS ของฟอร์มจะทำงานเลย — **ผ่าน** ตามเกณฑ์ "ต้องไม่บันทึกและต้องขึ้นข้อความบอก" แต่มีข้อสังเกตเล็กน้อย: ข้อความที่ขึ้นเป็นข้อความ native ของเบราว์เซอร์ภาษาอังกฤษ ("Please fill out this field.") ไม่ใช่ข้อความไทยที่ระบบเขียนเอง (ต่างจากช่องอื่นที่ตรวจครบ 5 ช่องพร้อมกันด้วย JS แล้วขึ้นข้อความไทย "กรุณากรอกข้อมูลให้ครบทุกช่องก่อนบันทึก" ที่บรรทัด 130 ของ `js/new-leave-request.js`) — ไม่ขัดกับสเปกเพราะสเปกไม่ได้ระบุภาษาของข้อความเตือนกรณีนี้ไว้ชัดเจน แต่ไม่สอดคล้องกับหลักการ "ภาษาบนหน้าจอเป็นภาษาไทยทั้งหมด" ในหัวข้อ 0.2 ของสเปก ถือเป็นจุดเล็ก ๆ ที่ปรับปรุงได้

---

## US-03 — เปิดดูรายละเอียดใบลา

**ผลการทดสอบ: ผ่าน**

- แสดงครบทุกช่องตามสเปก: หัวข้อ, เหตุผล, ประเภทการลา, วันที่เริ่มลา/สิ้นสุด, ผู้ขอลา, ผู้อนุมัติ, สถานะ, วันที่ยื่น
- รายการความเห็นการอนุมัติเรียงจากเก่าไปใหม่ถูกต้อง (ตรวจกับใบ seed lr001 ที่มี 2 ความเห็น: 2026-09-01 13:40 ตามด้วย 2026-09-02 10:05)
- ใบที่ยังไม่มีผู้อนุมัติ (`approverId` ว่าง) แสดงข้อความ "ยังไม่ได้กำหนดผู้อนุมัติ" ถูกต้อง
- มีปุ่ม/ลิงก์ "กลับไปหน้ารายการ" ทำงานถูกต้อง

หมายเหตุ: หน้านี้มีส่วน "สรุปจาก AI" (ปุ่ม "ให้ AI ช่วยสรุปใบลา") เพิ่มเข้ามาด้วย ซึ่งไม่ได้อยู่ใน accept criteria ของ US-03/US-09 ในหัวข้อ 3 ของสเปกโดยตรง แต่หัวข้อ 8 (แผนสัปดาห์ที่ 8) มีระบุ "สรุปใบลาให้หัวหน้าอ่านก่อนกดอนุมัติ" ไว้เป็นสโคปเดียวกับ AI จัดประเภท จึงไม่ถือว่าเป็นการทำงานล่วงหน้าเกินสเปก ไม่ได้ทดสอบละเอียดเพราะไม่มี acceptance criteria ให้อ้างอิงชัดเจน

---

## US-04 — เปลี่ยนสถานะใบลา (บัญชี manager)

**ผลการทดสอบ: ผ่าน**

- บัญชี manager เห็นใบลาของทุกคน (ทั้ง seed data และใบลาที่ tester employee สร้าง) ตรงตามสเปก
- ปุ่ม "อนุมัติ" และ "ไม่อนุมัติ" ปรากฏเฉพาะใบที่สถานะ "รอพิจารณา"
- กด "อนุมัติ" แล้วสถานะเปลี่ยนเป็น "อนุมัติ" ทันทีบนหน้าจอ และยืนยันด้วยการโหลดหน้าใหม่ (reload) ว่าเปลี่ยนจริงในฐานข้อมูล
- ทดสอบเช่นเดียวกันกับปุ่ม "ไม่อนุมัติ" (ดูรายละเอียดเพิ่มเติมใน US-05 เพราะเกี่ยวข้องกับกฎ "ต้องมีความเห็นก่อน")
- หลังเปลี่ยนสถานะแล้ว ปุ่มอนุมัติ/ไม่อนุมัติหายไปทั้งคู่ พร้อมข้อความ "ใบนี้พิจารณาผลแล้ว จึงเปลี่ยนสถานะหรือลบต่อไม่ได้" — กดซ้ำไม่ได้จริงเพราะไม่มีปุ่มให้กดแล้ว (ตรวจสอบว่าฟิลด์อื่น เช่น หัวข้อ/เหตุผล/วันที่/ผู้ขอลา/ความเห็นเดิม ไม่ถูกเขียนทับหลังเปลี่ยนสถานะ)

---

## US-05 — เขียนความเห็นการอนุมัติ

**ผลการทดสอบ: ผ่าน**

- มีกล่องพิมพ์ข้อความและปุ่ม "ส่งความเห็น" ในหน้ารายละเอียด
- กดส่งความเห็นแบบข้อความว่างเปล่า → ขึ้น alert dialog เตือน "กรุณาพิมพ์ข้อความความเห็นก่อนกดส่ง" และไม่มีความเห็นถูกบันทึก
- ส่งความเห็นที่มีข้อความจริงแล้วแสดงผลถูกต้องทันที ครบทั้งชื่อผู้เขียน (ชื่อไทยจริงของบัญชีที่ล็อกอิน), ข้อความ, วันเวลา
- ตรวจสอบกฎธุรกิจเพิ่มเติมตามหัวข้อ 6 ของสเปก ("การเปลี่ยนเป็นไม่อนุมัติ ต้องเขียนความเห็นอย่างน้อย 1 รายการก่อน"): สร้างใบลาใหม่ที่ยังไม่มีความเห็นเลย แล้วกด "ไม่อนุมัติ" ทันที → ระบบขึ้น alert "ต้องเขียนความเห็นอย่างน้อย 1 รายการก่อน จึงจะกดไม่อนุมัติได้" และไม่เปลี่ยนสถานะ พอเขียนความเห็นแล้วกด "ไม่อนุมัติ" อีกครั้ง จึงเปลี่ยนสถานะสำเร็จ — ตรงตามสเปกทุกประการ

---

## US-06 — จัดการประเภทการลา (บัญชี hr)

**ผลการทดสอบ: ผ่าน**

- หน้า `leave-types.html` แสดงตารางประเภทการลาทั้งหมด (ลาพักร้อน/ลาป่วย/ลากิจ) พร้อมปุ่มแก้ไข/ลบในแต่ละแถว และช่องกรอกชื่อประเภทใหม่ + ปุ่มเพิ่ม
- เพิ่มประเภทใหม่ "ลาทดสอบ QA" → ปรากฏในตารางทันที และปรากฏใน dropdown ของหน้า `new-leave-request.html` ทันทีโดยไม่ต้องรีเฟรชแคช (เปิดหน้าฟอร์มใหม่ก็เห็น เพราะอ่านจาก Firestore สด)
- แก้ไขชื่อประเภท (inline edit มีปุ่ม "บันทึก"/"ยกเลิก") → เปลี่ยนชื่อสำเร็จ พร้อมยืนยัน dialog "ยืนยันการลบ..." ตอนลบ
- ลบประเภททดสอบสำเร็จ ตารางอัปเดตทันที (ทำความสะอาดข้อมูลทดสอบเรียบร้อย ไม่เหลือ "ลาทดสอบ QA" ในระบบ)
- ทดสอบสิทธิ์: บัญชี manager เข้าหน้านี้ตรง ๆ ผ่าน URL → ขึ้นข้อความ "คุณไม่มีสิทธิ์เข้าหน้านี้ — จัดการประเภทการลาได้เฉพาะฝ่ายบุคคลเท่านั้น" พร้อมลิงก์กลับหน้าแรก ถูกต้องตามสเปก (ไม่ได้ทดสอบซ้ำกับบัญชี employee แต่คาดว่าพฤติกรรมเดียวกันเพราะใช้ guard เดียวกัน — เป็นข้อสังเกตเล็กน้อย ไม่ใช่บั๊ก)

---

## US-07 — ลบใบลาของตัวเอง

**ผลการทดสอบ: ผ่าน**

- ปุ่ม "ลบใบลานี้" ปรากฏเฉพาะใบของตัวเองที่สถานะยังเป็น "รอพิจารณา"
- กดลบแล้วมี `confirm()` dialog ถามยืนยันเสมอ ข้อความ: "ยืนยันการลบใบลา "..." หรือไม่ — ลบแล้วกู้คืนไม่ได้"
- กด Cancel บน dialog แล้วใบลายังอยู่ (ทดสอบแล้ว ไม่ถูกลบ)
- กด OK แล้วใบลาหายจากฐานข้อมูลจริง (ยืนยันด้วยการกลับไปหน้ารายการแล้วไม่เห็นแถวนั้นอีก) และหน้าจอ redirect กลับหน้ารายการอัตโนมัติ
- ใบที่สถานะเปลี่ยนเป็น "อนุมัติ"/"ไม่อนุมัติ" แล้ว ไม่มีปุ่มลบให้กดอีกต่อไป (ตรวจสอบกับใบที่ถูกอนุมัติระหว่างทดสอบ US-04) — ตรงตามสเปก

---

## US-08 — ความปลอดภัย / ล็อกอินและเห็นเฉพาะใบลาของตัวเอง

**ผลการทดสอบ: ผ่าน**

- ไม่ล็อกอินแล้วเข้า `leave-requests.html`, `new-leave-request.html`, `leave-types.html`, `leave-request-detail.html?id=lr001`, `dashboard.html` ทุกหน้าถูก redirect ไป `login.html` ทั้งหมด (บางหน้าใช้เวลาประมาณ 1-2 วินาทีก่อน redirect เพราะรอ Firebase Auth ตรวจสถานะก่อน)
- บัญชี employee (tester) เปิดใบลาของ employee คนอื่นไม่ได้: ทดสอบเปิด `leave-request-detail.html?id=lr001` (ใบของ "สมชาย ใจดี" ซึ่งไม่ใช่บัญชี tester) ขณะล็อกอินเป็น tester-employee → หน้าค้างที่ "กำลังโหลดข้อมูล..." ชั่วครู่ แล้วขึ้นข้อความ "โหลดข้อมูลใบลาไม่สำเร็จ: Missing or insufficient permissions." ยืนยันว่า Security Rules ปฏิเสธการอ่านจริง ไม่ใช่แค่ซ่อนปุ่มฝั่ง UI
- บัญชี manager และ hr เปิดใบลาของทุกคนได้ปกติ (ทดสอบกับ lr001 ทั้งสองบัญชี) ตรงตามสเปก
- บัญชี employee (tester) เข้า `leave-requests.html` เห็นเฉพาะใบของตัวเองเท่านั้น (ตอนบัญชียังไม่มีใบลา ขึ้น "ยังไม่มีใบขอลาในระบบ" ทั้งที่ในระบบมีใบ seed อยู่ 5 ใบของคนอื่น ยืนยันว่ากรองตามสิทธิ์จริง)

หมายเหตุเล็กน้อย (ไม่ใช่บั๊ก แต่บันทึกไว้): ทุกครั้งที่เปิดหน้าที่ต้อง login ในสถานะยังไม่ login จะมี console error `Error: ยังไม่ได้ล็อกอิน` จาก `js/auth-guard.js:40` ปรากฏก่อน redirect เสมอ ถือเป็นพฤติกรรมที่ตั้งใจของ auth-guard (โยน error เพื่อสกัดกั้น) ไม่กระทบการทำงาน จึงไม่นับเป็นข้อผิดพลาด

### เพิ่มเติม (ทดสอบความปลอดภัยเจาะจง 2 ตัว, 2026-09-20) — ผ่านทั้งคู่

**เทสต์ 1 — ไม่ล็อกอินแล้วเปิดหน้ารายการ ต้องอ่านข้อมูลไม่ได้**
- ระดับ UI: เข้า `leave-requests.html` ตรง ๆ โดยไม่ได้ล็อกอิน → ถูก redirect ไป `login.html` ทันที (auth-guard)
- ระดับข้อมูลจริง (เข้มกว่าการเช็กแค่ UI): ยิง request ตรงไปยัง Firestore REST API แบบไม่มี auth token เลย —
  `GET https://firestore.googleapis.com/v1/projects/leaveeasy-oneshot-82d59/databases/(default)/documents/leaveRequests`
  ผลลัพธ์: **HTTP 403** `{"error":{"code":403,"message":"Missing or insufficient permissions.","status":"PERMISSION_DENIED"}}`
  ยืนยันว่าการกันไว้เป็นเรื่องของ Firestore Security Rules จริง ๆ ไม่ใช่แค่ redirect ฝั่ง UI ที่ยังพอเลี่ยงได้ถ้าเรียก API ตรง — **ผ่าน**

**เทสต์ 2 — ล็อกอินด้วยบัญชีที่สอง แล้วเปิดใบลาของบัญชีแรก ต้องเปิดไม่ได้**
- สมัครบัญชี employee ใหม่ `leaveeasy.tester.employee2@example.com` (บัญชีที่สอง ไม่เกี่ยวข้องกับใบลาใด ๆ)
- ใบลาของบัญชีแรก (`leaveeasy.tester.employee@example.com`) ที่ใช้ทดสอบ: doc id `bUxo7DvJQBICvIzHoQI2` ("ทดสอบหลัง AI ล้มเหลว ยังบันทึกได้")
- ล็อกอินด้วยบัญชีที่สอง แล้วเปิด `leave-request-detail.html?id=bUxo7DvJQBICvIzHoQI2` ตรง ๆ ผ่าน URL
- ผลลัพธ์: เปิดไม่ได้จริง ขึ้นข้อความ "โหลดข้อมูลใบลาไม่สำเร็จ: Missing or insufficient permissions." ซึ่งเป็น permission error จริงจาก Firestore rules (ไม่ใช่หน้าจอเปล่า ๆ ที่แค่ซ่อนข้อมูลไว้) — **ผ่าน**

---

## US-09 — ปุ่มให้ AI จัดประเภทการลาอัตโนมัติ

**ผลการทดสอบ: ผ่าน** (แก้ไขผลจากการตรวจครั้งแรกที่รายงานผิดพลาด — ดูหัวข้อ "แก้ไขผลการทดสอบ" ด้านล่าง)

### ผ่าน
- ปุ่ม "ให้ AI ช่วยจัดประเภทการลา" อยู่ในหน้า `new-leave-request.html` จริง
- กดแล้วระบบส่งข้อความในช่องเหตุผลไปยัง AI จริง (เรียก `https://openrouter.ai/api/v1/chat/completions`) แล้วเลือกประเภทที่ตรงบริบทให้ถูกต้อง — ทดสอบ 3 กรณี ("มีไข้สูงและปวดหัวมาก...", "ปวดท้องมากต้องไปโรงพยาบาลด่วน", "ปวดฟันมากต้องไปหาหมอฟันด่วน") ทั้งหมดเลือก "ลาป่วย" ถูกต้อง ซึ่งเป็นประเภทที่มีอยู่จริงในระบบ
- มีป้ายกำกับ "ข้อเสนอจาก AI — โปรดตรวจสอบก่อนยืนยัน" ปรากฏถูกต้องหลัง AI ตอบกลับ
- ผู้ใช้แก้ไข dropdown ที่ AI เลือกให้เองได้ตามปกติ (ทดสอบเปลี่ยนจาก "ลาป่วย" เป็น "ลากิจ" ได้ทันที)
- จำลองกรณีเรียก AI ไม่สำเร็จ (block request ด้วย Playwright route interception ให้ abort) → ระบบไม่ค้าง ขึ้นข้อความ "เรียก AI ไม่สำเร็จ กรุณาเลือกประเภทการลาเอง" ให้ผู้ใช้เลือกเอง และยังกดปุ่ม "บันทึก" เพื่อสร้างใบลาได้ตามปกติ (ทดสอบสร้างใบลาสำเร็จหลังจากนี้จริง)

### เวลาตอบสนองกรณีล้มเหลว (ข้อสังเกต ไม่ใช่บั๊ก)
- ในการทดสอบครั้งแรก สังเกตว่ากรณี route ถูก abort คำขอ AI ใช้เวลาประมาณ 13.2 วินาทีกว่าจะขึ้น error message ซึ่งยังอยู่ในกรอบ "ไม่เกิน 15 วินาที" ของสเปก แต่ใกล้เพดานมาก ควรตรวจสอบว่า network ที่ช้ากว่านี้เล็กน้อยจะทำให้เกิน 15 วินาทีหรือไม่ (ค่านี้ขึ้นกับ `setTimeout` 15000ms ใน `js/new-leave-request.js` บรรทัด 201-203 ซึ่งเป็นการตั้ง timeout ของฝั่งเรา ไม่ใช่ของ AI เอง จึงชนเพดาน 15 วิพอดีเสมอในกรณีเรียกไม่สำเร็จแบบ hang ค้าง)

---

## แก้ไขผลการทดสอบ (2026-09-20) — เกณฑ์ "ปุ่มกดซ้ำไม่ได้ระหว่างรอ AI"

การทดสอบครั้งแรกรายงานว่าปุ่ม "ให้ AI ช่วยจัดประเภทการลา" ไม่ถูก disable ระหว่างรอผล และคลิกรัว 3 ครั้งทำให้มี request ยิงไปพร้อมกัน 3 ครั้ง — **ผลนี้เป็นการรายงานผิดพลาด ตรวจสอบซ้ำแล้วพบว่าไม่จริง**

หลักฐานการตรวจซ้ำ (อ่านโค้ดในไฟล์ที่ deploy จริงก่อน แล้วทดสอบยืนยันบนเว็บจริงอีกครั้ง):
- อ่าน `js/new-leave-request.js` (เทียบไฟล์ที่ deploy จริงกับไฟล์ในโปรเจกต์ด้วย `diff` แล้วเหมือนกันทุกตัวอักษร — ไม่ใช่ deploy คนละเวอร์ชัน) พบว่าโค้ดตั้ง `ปุ่มAIจัดประเภท.disabled = true` และเปลี่ยน `.textContent = "กำลังจัดประเภท..."` แบบ synchronous ตั้งแต่บรรทัดแรก ๆ ของ handler ก่อนเจอ `await fetch(...)` เลย
- ทดสอบบนเว็บจริงด้วย `browser_evaluate` เรียก `button.click()` ติดกัน 3 ครั้งในสคริปต์เดียว (ไม่มี round-trip คั่นระหว่างครั้ง เพื่อตัดปัญหา timing จากการเรียกเครื่องมือแยกกัน) ผลคือ: หลังคลิกครั้งแรก `disabled` เปลี่ยนเป็น `true` และข้อความเปลี่ยนเป็น "กำลังจัดประเภท..." ทันที ส่วนคลิกครั้งที่ 2 และ 3 ไม่มีผลใด ๆ เพิ่มเติมเลย (ปุ่มที่ `disabled = true` ไม่ยอมรับ `.click()` ซ้ำ ตามพฤติกรรมมาตรฐานของเบราว์เซอร์)
- ตรวจ network log คู่กันพบว่ามี request ไปยัง `openrouter.ai/api/v1/chat/completions` แค่ **1 ครั้งเท่านั้น** ต่อการคลิก 1 ชุด ไม่ใช่ 3 ครั้ง

**สรุป:** เกณฑ์ "ระหว่างรอ ปุ่มขึ้นว่ากำลังทำงานและกดซ้ำไม่ได้" **ผ่าน** จริง ๆ ไม่ใช่บั๊ก ขอโทษด้วยที่รายงานรอบแรกผิดพลาด — สาเหตุที่น่าจะทำให้รอบแรกเห็น request ซ้ำ 3 ครั้งยังไม่ทราบแน่ชัด (อาจเกี่ยวกับวิธีคลิกที่ใช้ในรอบแรกซึ่งต่างจากรอบนี้) แต่จากโค้ดและผลทดสอบซ้ำยืนยันว่าใน UI จริงปุ่มกันการกดซ้ำได้ถูกต้อง

---

## ข้อสังเกตเพิ่มเติม (นอกเหนือจาก Acceptance Criteria แต่เกี่ยวข้องกับความปลอดภัย)

**พบว่า OpenRouter API key ถูกส่งออกไปตรง ๆ จากฝั่งเบราว์เซอร์ (client-side) แบบเปิดเผย**

- ขณะทดสอบปุ่ม AI จัดประเภทการลา ตรวจสอบ network request ไปยัง `https://openrouter.ai/api/v1/chat/completions` พบ request header:
  `authorization: Bearer sk-or-v1-****` (คีย์เต็มมองเห็นได้จริงใน DevTools ของผู้ใช้ ไม่ได้ปิดบังในเว็บจริง แต่ไม่ระบุค่าคีย์เต็มไว้ในเอกสารนี้เพื่อไม่ให้หลุดขึ้น GitHub)
- เนื่องจากระบบนี้ไม่มี backend ของตัวเอง (ตามสถาปัตยกรรมที่สเปกกำหนด) การเรียก OpenRouter จึงทำจาก JavaScript ฝั่งเบราว์เซอร์โดยตรง ทำให้ API key รั่วไหลอยู่ใน network traffic ที่ผู้ใช้ทุกคน (รวมถึง employee) สามารถเปิด DevTools ดูได้ และนำ key นี้ไปใช้เบิกโควตา/ค่าใช้จ่ายของบัญชี OpenRouter ได้โดยไม่ต้องผ่านระบบเลย
- นี่ไม่ใช่เกณฑ์การยอมรับข้อใดใน US-09 โดยตรง แต่เป็นความเสี่ยงด้านความปลอดภัยที่ชัดเจนและตรงกับข้อกังวลที่ระบุไว้ใน `CLAUDE.md` ของโปรเจกต์เรื่องห้ามเปิดเผยคีย์ลับ จึงบันทึกไว้ให้ผู้ดูแลโปรเจกต์พิจารณา
- **ไม่ได้แก้ไขไฟล์ใด ๆ เพื่อปิดช่องโหว่นี้** ตามขอบเขตงานของ tester agent

---

## สรุปผลรวม

| User Story | ผลลัพธ์ |
|---|---|
| US-01 ดูรายการใบลาทั้งหมด | ผ่าน |
| US-02 ยื่นใบขอลาใหม่ | ผ่าน |
| US-03 เปิดดูรายละเอียดใบลา | ผ่าน |
| US-04 เปลี่ยนสถานะใบลา | ผ่าน |
| US-05 เขียนความเห็นการอนุมัติ | ผ่าน |
| US-06 จัดการประเภทการลา | ผ่าน |
| US-07 ลบใบลาของตัวเอง | ผ่าน |
| US-08 ความปลอดภัย/ล็อกอิน | ผ่าน |
| US-09 ปุ่ม AI จัดประเภทการลา | ผ่าน (แก้ไขจากรายงานรอบแรกที่ระบุผิดว่าเกณฑ์ "ปุ่มกดซ้ำไม่ได้ระหว่างรอ" ไม่ผ่าน) |

ไม่มีรายการที่ต้องข้าม เนื่องจากได้รับบัญชีทดสอบครบทั้ง 3 บทบาท (employee, manager, hr) พร้อมรหัสผ่านใช้งานได้จริงทั้งหมด
