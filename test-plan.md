# ชุดทดสอบ LeaveEasy (Test Plan)

> ไฟล์นี้คือ **ชุดทดสอบที่ใช้ซ้ำได้** (test cases) แยกจาก `test-report.md` ซึ่งเป็นผลการรันครั้งล่าสุด
> อิงเกณฑ์การยอมรับใน `leaveeasy-spec.md` หัวข้อ 3 (US-01 ถึง US-09) บวกกับเทสต์เพิ่มเติมที่ไม่ได้อยู่ในสเปกโดยตรงแต่ตกลงเพิ่มไว้ระหว่างทำ QA
>
> **วิธีใช้:** สั่งให้ agent `tester` (Playwright MCP) รันตามรายการนี้ทีละข้อกับ URL ที่ deploy จริง แล้วบันทึกผลใหม่ลง `test-report.md`
> **บัญชีทดสอบที่ต้องมี:** ดูหัวข้อ "บัญชีทดสอบ" ด้านล่าง — ถ้ายังไม่มีให้สมัครผ่าน signup.html แล้วขอให้เจ้าของโปรเจกต์ช่วยตั้ง `role` ใน Firestore Console (สมัครใหม่จะได้ `employee` เสมอ)

---

## บัญชีทดสอบ

| อีเมล | role | ใช้ทดสอบอะไร |
|---|---|---|
| `leaveeasy.tester.employee@example.com` | employee | ผู้ขอลา (US-01, 02, 03, 05, 07, 09) |
| `leaveeasy.tester.employee2@example.com` | employee | บัญชีที่สอง ใช้ยืนยันว่า employee เปิดใบของ employee คนอื่นไม่ได้ |
| `leaveeasy.tester.manager@example.com` | manager | ผู้อนุมัติ (US-04, 05, 08) |
| `leaveeasy.tester.hr@example.com` | hr | ฝ่ายบุคคล (US-06, 08) |

รหัสผ่านทุกบัญชี: `TesterPass123`

---

## US-01 — ดูรายการใบลาทั้งหมด

1. ล็อกอินด้วยบัญชีที่มีใบลาอยู่แล้ว → เปิด `leave-requests.html`
   - คาดหวัง: ตารางมีคอลัมน์ หัวข้อ · ประเภทการลา · สถานะ · ผู้ขอลา · วันที่ลา ครบ
   - คาดหวัง: ชื่อผู้ขอลาเป็นภาษาไทย ไม่ใช่รหัสเอกสาร
   - คาดหวัง: ป้ายสถานะสีถูกต้อง (รอพิจารณา=เหลือง, อนุมัติ=เขียว, ไม่อนุมัติ=แดง)
2. กดแถวใดแถวหนึ่ง → คาดหวัง: ไปหน้า `leave-request-detail.html?id=<docId>` ของใบนั้นจริง
3. ล็อกอินด้วยบัญชี employee ใหม่ที่ยังไม่มีใบลา → คาดหวัง: ขึ้นข้อความ "ยังไม่มีใบขอลาในระบบ" ไม่ใช่ตารางว่าง

## US-02 — ยื่นใบขอลาใหม่

1. เปิด `new-leave-request.html` → คาดหวัง: มีช่องหัวข้อ, เหตุผล (textarea), ประเภทการลา (dropdown จาก Firestore จริง), วันที่เริ่มลา, วันที่สิ้นสุด
2. กรอกครบทุกช่องแล้วกดบันทึก → คาดหวัง: มีใบลาใหม่เกิดขึ้นจริงใน Firestore, redirect กลับหน้ารายการ, สถานะเริ่มต้นเป็น "รอพิจารณา", มี `createdAt` อัตโนมัติ
3. กรอกหัวข้อไว้แล้วกดยกเลิก → คาดหวัง: กลับหน้ารายการโดยไม่บันทึก
4. **กรอกครบทุกช่องยกเว้น "หัวข้อ" แล้วกดบันทึก** → คาดหวัง: ไม่บันทึกข้อมูล และมีข้อความ/ตัวบ่งชี้แจ้งผู้ใช้ว่ากรอกไม่ครบ (ปัจจุบันใช้ HTML5 `required` ทำให้เบราว์เซอร์ขึ้น tooltip เอง — ยอมรับได้ แต่เป็นภาษาอังกฤษ ไม่ใช่ข้อความไทยแบบช่องอื่น)

## US-03 — เปิดดูรายละเอียดใบลา

1. เปิดใบลาที่มีความเห็นอยู่แล้ว (เช่น seed `lr001`) → คาดหวัง: แสดงครบทุกช่อง (หัวข้อ, เหตุผล, ประเภท, วันที่เริ่ม/สิ้นสุด, ผู้ขอลา, ผู้อนุมัติ, สถานะ, วันที่ยื่น) และรายการความเห็นเรียงเก่า→ใหม่ถูกต้อง
2. เปิดใบที่ `approverId` ว่าง → คาดหวัง: ขึ้น "ยังไม่ได้กำหนดผู้อนุมัติ"
3. กดปุ่มกลับหน้ารายการ → คาดหวัง: กลับ `leave-requests.html`

## US-04 — เปลี่ยนสถานะใบลา (บัญชี manager/hr)

1. ล็อกอิน manager → เปิดใบที่สถานะ "รอพิจารณา" → คาดหวัง: มีปุ่มอนุมัติ/ไม่อนุมัติ
2. กดอนุมัติ → คาดหวัง: สถานะเปลี่ยนทันทีบนจอ และคงอยู่หลัง reload, ปุ่มอนุมัติ/ไม่อนุมัติหายไปหลังจากนั้น (เปลี่ยนต่อไม่ได้)
3. ตรวจว่าฟิลด์อื่น (หัวข้อ/เหตุผล/วันที่/ผู้ขอลา) ไม่ถูกเขียนทับหลังเปลี่ยนสถานะ

## US-05 — เขียนความเห็นการอนุมัติ

1. ส่งความเห็นว่างเปล่า → คาดหวัง: มีข้อความเตือน ไม่บันทึก
2. ส่งความเห็นจริง → คาดหวัง: แสดงชื่อผู้เขียน/ข้อความ/เวลาใหม่ทันที
3. สร้างใบใหม่ที่ยังไม่มีความเห็น แล้วกด "ไม่อนุมัติ" ทันที → คาดหวัง: ระบบเตือนว่าต้องมีความเห็นอย่างน้อย 1 รายการก่อน ไม่เปลี่ยนสถานะ; เขียนความเห็นแล้วกดอีกครั้งจึงเปลี่ยนสำเร็จ

## US-06 — จัดการประเภทการลา (บัญชี hr)

1. เปิด `leave-types.html` ด้วยบัญชี hr → คาดหวัง: ตารางประเภทการลาครบ พร้อมช่องเพิ่ม/ปุ่มแก้ไข/ลบ
2. เพิ่มประเภทใหม่ → คาดหวัง: ปรากฏในตารางทันที และใน dropdown ของ `new-leave-request.html`
3. แก้ไข/ลบประเภททดสอบ → คาดหวัง: อัปเดตทันที, ลบแล้วไม่เหลือในระบบ (ทำความสะอาดข้อมูลทดสอบทุกครั้ง)
4. เปิด `leave-types.html` ด้วยบัญชี manager หรือ employee → คาดหวัง: ขึ้นข้อความไม่มีสิทธิ์ ไม่ใช่หน้าเปล่าหรือ error ดิบ

## US-07 — ลบใบลาของตัวเอง

1. เปิดใบของตัวเองที่ "รอพิจารณา" → คาดหวัง: มีปุ่มลบ
2. กดลบ → คาดหวัง: มี confirm dialog เสมอ; กด Cancel แล้วใบยังอยู่
3. กด OK → คาดหวัง: ใบหายจาก Firestore จริง, redirect กลับหน้ารายการ
4. เปิดใบที่สถานะ "อนุมัติ"/"ไม่อนุมัติ" แล้ว → คาดหวัง: ไม่มีปุ่มลบ

## US-08 — ล็อกอินและสิทธิ์แยกตามบทบาท (รวมเทสต์ความปลอดภัย)

1. ไม่ล็อกอินแล้วเปิดทุกหน้าที่ต้อง auth (`leave-requests.html`, `new-leave-request.html`, `leave-types.html`, `leave-request-detail.html?id=...`, `dashboard.html`) → คาดหวัง: ถูก redirect ไป `login.html` ทุกหน้า
2. **[ความปลอดภัยระดับข้อมูล]** ไม่ล็อกอิน ยิง request ตรงไปยัง Firestore REST API โดยไม่มี auth token:
   `GET https://firestore.googleapis.com/v1/projects/<project-id>/databases/(default)/documents/leaveRequests`
   → คาดหวัง: **HTTP 403 PERMISSION_DENIED** (ยืนยันว่า security rules กันจริง ไม่ใช่แค่ UI redirect)
3. **[ความปลอดภัยข้ามบัญชี]** ล็อกอินด้วยบัญชี employee คนที่ 2 (`employee2`) แล้วเปิด `leave-request-detail.html?id=<docId ของใบของ employee คนแรก>` ตรงผ่าน URL → คาดหวัง: เปิดไม่ได้ ขึ้น error "Missing or insufficient permissions" จริงจาก Firestore (ไม่ใช่แค่ซ่อน UI)
4. ล็อกอิน manager/hr → เปิดใบของใครก็ได้ → คาดหวัง: เปิดได้ปกติ
5. ล็อกอิน employee → เปิด `leave-requests.html` → คาดหวัง: เห็นเฉพาะใบของตัวเอง แม้ในระบบมีใบของคนอื่นอยู่

## US-09 — ปุ่ม AI จัดประเภทการลาอัตโนมัติ

1. พิมพ์เหตุผลการลาที่สื่อถึงประเภทใดประเภทหนึ่งชัดเจน แล้วกด "ให้ AI ช่วยจัดประเภทการลา" → คาดหวัง: เลือกประเภทที่มีอยู่จริงในระบบให้ถูกต้อง พร้อมป้าย "ข้อเสนอจาก AI — โปรดตรวจสอบก่อนยืนยัน"
2. แก้ไข dropdown ที่ AI เลือกให้เอง → คาดหวัง: แก้ได้เสมอ
3. จำลองการเรียก AI ไม่สำเร็จ (เช่น block request) → คาดหวัง: ไม่ค้าง ขึ้นข้อความแจ้งให้เลือกเอง และยังกดบันทึกใบลาได้ปกติ ภายในเวลาไม่เกิน 15 วินาที
4. **[กันกดซ้ำ]** ระหว่างรอผล AI ให้คลิกปุ่มซ้ำหลายครั้งติดกัน (ทดสอบด้วย `button.click()` ต่อกันในสคริปต์เดียว เพื่อตัด timing ของเครื่องมือทดสอบเอง ไม่ใช่คลิกแยกรอบเป็นเครื่องมือคนละคำสั่ง) → คาดหวัง: ปุ่ม `disabled` ทันทีหลังคลิกแรก ข้อความเปลี่ยนเป็น "กำลังจัดประเภท..." และมี request ไปยัง OpenRouter แค่ 1 ครั้งเท่านั้น
5. ปล่อยช่องเหตุผลว่างแล้วกดปุ่ม AI → คาดหวัง: เตือนให้กรอกเหตุผลก่อน ไม่เรียก AI

## ข้อสังเกตด้านความปลอดภัยที่ควรเช็กซ้ำทุกรอบ (ไม่ใช่ acceptance criteria แต่ risk ที่รู้จักแล้ว)

- ตรวจ network request ตอนกดปุ่ม AI ว่า `Authorization: Bearer <key>` ของ OpenRouter ยังคงรั่วออกมาให้เห็นได้จาก DevTools ของทุกบทบาท (รวม employee) หรือไม่ — ถ้ายังไม่มี backend คั่นกลาง ค่านี้จะเป็น `true` เสมอ เป็นความเสี่ยงที่รู้อยู่แล้ว ไม่ใช่เกณฑ์ pass/fail ของ US-09 แต่ควรรายงานไว้ทุกรอบจนกว่าจะแก้สถาปัตยกรรม

---

## ขอบเขตที่ไม่ต้องทดสอบใน Module 2

US-10 (ค้นหา/กรอง/เรียง), US-11 (แดชบอร์ดข้อมูลจริง), US-12 (แนบไฟล์) อยู่นอกสโคป Module 2 ตาม `leaveeasy-spec.md` หัวข้อ 8-9 — ถ้าพบว่าฟีเจอร์เหล่านี้ถูกทำไว้แล้วทั้งที่ไม่ควร ให้รายงานเป็นปัญหา "ทำงานล่วงหน้าเกินสเปก" ไม่ใช่เพิ่มเป็นเทสต์ใหม่
