# CLAUDE.md

ไฟล์นี้ให้คำแนะนำแก่ Claude Code (claude.ai/code) เวลาทำงานกับโค้ดในโปรเจกต์นี้

## โปรเจกต์นี้คืออะไร

LeaveEasy (🔧 ระบบขอลาออนไลน์) เป็นโปรเจกต์ Workshop สำหรับผู้เรียน ADT-RAISE Non-Degree Batch 2, Module 2 (สัปดาห์ที่ 6–9) เป็น prototype ระบบขอลาที่เขียนด้วย **HTML/CSS/JavaScript ธรรมดา** — ไม่มี framework ไม่มีขั้นตอน build ผู้ใช้กำลังค่อย ๆ ต่อระบบเข้ากับ Firebase/Firestore ทีละสัปดาห์ตลอด 4 สัปดาห์ของโมดูลนี้

**`leaveeasy-spec.md` คือเอกสารต้นฉบับ (source of truth) ของโปรเจกต์นี้** กำหนดหน้าจอ ชื่อช่องข้อมูล บทบาทผู้ใช้ สถานะ และเกณฑ์การยอมรับของแต่ละสัปดาห์ไว้ชัดเจน ให้อ่านไฟล์นี้ก่อนลงมือทำฟีเจอร์ใด ๆ — **ห้ามเพิ่มสิ่งที่สเปกไม่ได้ระบุ และห้ามทำงานของสัปดาห์ถัดไปล่วงหน้า** (สเปกห้ามไว้ชัดเจนทั้งสองข้อ ดูหัวข้อ 0.2/8/9 ในไฟล์นั้น) ถ้ามีคำสั่งที่ขัดกับสเปก ให้ทักท้วงก่อน อย่าลงมือทำเกินขอบเขตไปเอง

## คำสั่งที่ใช้บ่อย

```
npm run dev     # เปิดเว็บแบบ static ที่ http://localhost:3000 (ใช้แพ็กเกจ `serve`)
```

ไม่มีขั้นตอน build, lint หรือ test — โปรเจกต์นี้เป็น HTML/JS แบบ static ที่เสิร์ฟตรง ๆ ไม่ผ่าน bundler

## สถาปัตยกรรมของระบบ

**หลายหน้า ไม่มี bundler** แต่ละหน้าจอเป็นไฟล์ `.html` ของตัวเอง โหลดสคริปต์ที่ใช้ร่วมกันบวกกับสคริปต์เฉพาะหน้านั้น:

- `index.html` — หน้าแรก แสดงคำอธิบายบทบาท/สถานะ
- `leave-requests.html` → `js/leave-requests.js` — รายการใบลาทั้งหมด (อ่านจาก Firestore collection `leaveRequests` จริงแล้ว)
- `new-leave-request.html` → `js/new-leave-request.js` — ฟอร์มยื่นใบลา (ตอนนี้เก็บในหน่วยความจำ/`sessionStorage` เท่านั้น ยังไม่บันทึกลง Firestore)
- `leave-request-detail.html` → `js/leave-request-detail.js` — รายละเอียดใบลา 1 ใบ + ความเห็นการอนุมัติ (ตอนนี้อ่านจากข้อมูลปลอมใน `js/data.js` บวกกับรายการใน `sessionStorage` ยังไม่ใช่ Firestore)
- `leave-types.html` → `js/leave-types.js` — เพิ่ม/แก้/ลบ ประเภทการลา (ตอนนี้อยู่ในหน่วยความจำเท่านั้น)
- `seed.html` → `js/seed.js` — ปุ่มสำหรับใส่ข้อมูลปลอมจาก `js/data.js` ขึ้น Firestore ครั้งเดียว ไม่ได้เป็นส่วนหนึ่งของการใช้งานปกติ

สคริปต์ที่ใช้ร่วมกันทุกหน้า:
- `js/util.js` — `esc()` สำหรับ escape HTML, `ป้ายสถานะ()` สำหรับวาดป้ายสถานะ, `เวลาตอนนี้()` สำหรับจัดรูปแบบเวลา, `ค่าจากURL()` สำหรับอ่านค่าพารามิเตอร์จาก URL โหลดแบบ `<script defer>` ธรรมดา ไม่ใช่ module — ประกาศเป็นตัวแปร global
- `js/nav.js` — วาดแถบเมนูด้านบนลงใน `<div id="nav"></div>` และกำหนดฟังก์ชัน `showConfigWarning()` ที่ใช้ตอนยังไม่ได้ตั้งค่า Firestore
- `js/data.js` — ชุดข้อมูลปลอม/ข้อมูลตั้งต้น (`window.LEAVE_DATA`) และเป็นที่อ้างอิงชื่อช่องข้อมูลที่ถูกต้องบน Firestore
- `js/firebase-config.js` — ตั้งค่าเชื่อมต่อ Firebase/Firestore โหลดแบบ ES module (ผ่าน URL ของ CDN ไม่ได้ผ่าน npm bundling) ใช้โดยหน้าที่คุยกับ Firestore

**การย้ายไปใช้ Firestore ยังทำไปได้แค่บางหน้า ไม่ใช่ทั้งระบบพร้อมกัน:** ณ สัปดาห์ที่ 6 มีแค่ `leave-requests.html` ที่อ่านข้อมูลจริงจาก Firestore (แบบอ่านอย่างเดียว) ส่วนหน้าอื่น (`new-leave-request`, `leave-request-detail`, `leave-types`) ยังใช้ `window.LEAVE_DATA` จาก `js/data.js` ร่วมกับ `sessionStorage` สำหรับสิ่งที่ "บันทึก" ในเซสชันปัจจุบัน อย่าสมมติว่าทุกหน้าใช้แหล่งข้อมูลเดียวกัน — ต้องเช็กก่อนว่าไฟล์ที่กำลังแก้ต่อกับ Firestore แล้วหรือยัง

**โครงสร้างข้อมูลบน Firestore** (ดูชื่อช่องข้อมูลที่ถูกต้องได้จาก `js/data.js` และ `js/seed.js`):
- **ชื่อโฟลเดอร์ (collection) ทั้งหมดใน Firestore มี 4 ชื่อ:**
  - `users` — ผู้ใช้ 3 บทบาท
  - `leaveTypes` — ประเภทการลา
  - `leaveRequests` — ใบขอลา
  - `leaveRequests/{id}/approvals` — ความเห็นการอนุมัติ เป็น **subcollection ของแต่ละเอกสารใน `leaveRequests`** ไม่ใช่ collection ระดับบนสุด
  ห้ามตั้งชื่อ collection ใหม่ที่ไม่อยู่ในรายการนี้โดยไม่เช็ก `leaveeasy-spec.md` ก่อน
- แต่ละเอกสารเก็บชื่อคู่กับรหัสเสมอ (เช่น ใบลาเก็บทั้ง `requesterId` และ `requesterName`) เพราะ Firestore ไม่มี JOIN — ถ้าจะเพิ่มความสัมพันธ์ใหม่ ให้ทำตามรูปแบบนี้ อย่าเก็บแค่รหัสเปล่า ๆ
- ค่าของช่อง `role` ต้องเป็นภาษาอังกฤษเสมอ (`employee` / `manager` / `hr`) เพราะกฎเฝ้าข้อมูล (security rules) ของสัปดาห์ที่ 8 จะใช้ค่านี้ตรวจสิทธิ์ ส่วนข้อความภาษาไทยใช้แสดงบนหน้าจอเท่านั้น
- **ช่อง `status` ของใบลามี 3 ค่าคงที่เท่านั้น:** `รอพิจารณา` (เหลือง, ค่าเริ่มต้นของใบใหม่) → `อนุมัติ` (เขียว) หรือ `ไม่อนุมัติ` (แดง) ใบที่เปลี่ยนเป็น `อนุมัติ`/`ไม่อนุมัติ` แล้วเปลี่ยนสถานะต่อไม่ได้อีก — ห้ามเพิ่มค่าสถานะอื่นนอกเหนือจาก 3 ค่านี้

**ชื่อฟังก์ชันและตัวแปรเป็นภาษาไทย** ในไฟล์ JS ทั้งหมด (เช่น `แสดงตาราง`, `ป้ายสถานะ`, `เปลี่ยนสถานะ`) เป็นความตั้งใจให้ตรงกับกลุ่มผู้เรียนเป้าหมาย — เขียนโค้ดใหม่ให้สอดคล้องกับรูปแบบนี้ อย่าเปลี่ยนไปใช้ชื่อภาษาอังกฤษ

`firestore.rules` ตอนนี้เปิดอ่าน/เขียนแบบไม่ล็อกอินทั้งหมด (`allow read, write: if true`) โดยตั้งใจสำหรับสัปดาห์ที่ 6 (ยังไม่มีระบบล็อกอิน) — อย่ารัดกุมกฎนี้เองถ้าไม่มีใครสั่ง เพราะสเปกกำหนดให้กฎที่ต้องล็อกอินก่อนเริ่มใช้สัปดาห์ที่ 7 และกฎแยกตามบทบาทครบชุดเริ่มสัปดาห์ที่ 8

## ข้อห้ามสำคัญ

**ห้ามใส่คีย์หรือข้อมูลลับลงในไฟล์ที่จะ push ขึ้น GitHub เด็ดขาด** เช่น API key ของบริการอื่น, service account key, token, รหัสผ่าน — ถ้าต้องใช้ค่าลับจริง ๆ ให้เก็บไว้ในไฟล์ที่ถูก `.gitignore` กันไว้ (เช่นไฟล์ `.env`) ไม่ใช่ hardcode ลงไฟล์ที่ commit
- ข้อยกเว้นเดียวคือค่าใน `js/firebase-config.js` (`firebaseConfig`) ซึ่งเป็น **client config ของ Firebase ที่ตั้งใจให้เปิดเผยฝั่งเบราว์เซอร์ได้** (ความปลอดภัยจริงมาจาก Firestore security rules ไม่ใช่การซ่อนค่านี้) — อย่าย้ายค่านี้ไปไฟล์ลับ และอย่าใช้ไฟล์นี้เป็นตัวอย่างอ้างว่า "ใส่คีย์ลงไฟล์ได้"
- ถ้างานที่ทำต้องใช้คีย์ใหม่ (เช่น ต่อ AI ผู้ช่วยในสัปดาห์ที่ 8) ให้ถามผู้ใช้ก่อนว่าจะเก็บค่านั้นไว้ที่ไหน อย่าเดาแล้วเขียนลงไฟล์ที่ push เอง
