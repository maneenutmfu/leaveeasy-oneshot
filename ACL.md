# ACL — ตารางสิทธิ์ของระบบ LeaveEasy

> เอกสารนี้อธิบายตารางสิทธิ์ที่ `js/acl.js` (ฟังก์ชัน `ตรวจสิทธิ์(action, ctx)`) implement ไว้
> อิงตาม `leaveeasy-spec.md` หัวข้อ 2 (ผู้ใช้ 3 บทบาท) และหัวข้อ 6 (สถานะและการเปลี่ยนสถานะ)
>
> ⚠️ **นี่คือชั้นกัน UX เท่านั้น** (ซ่อนปุ่ม/เมนู/กรองรายการฝั่งหน้าเว็บ) **ไม่ใช่ตัวป้องกันข้อมูลจริง**
> ตัวป้องกันข้อมูลจริงอยู่ที่ `firestore.rules` เสมอ — ต่อให้ซ่อนปุ่มสำเร็จ แต่ถ้า rules ไม่ครอบ ก็ยังถูกเรียกตรง ๆ ได้

## บทบาท 3 แบบ

| บทบาท | ค่าที่เก็บในช่อง `role` |
|---|---|
| 🙋 ผู้ขอลา | `employee` |
| ✅ ผู้อนุมัติ | `manager` |
| 🧾 ฝ่ายบุคคล | `hr` |

## ตาราง action → สิทธิ์

| action | ผู้ขอลา (`employee`) | ผู้อนุมัติ (`manager`) | ฝ่ายบุคคล (`hr`) | เงื่อนไขเพิ่มเติม |
|---|:---:|:---:|:---:|---|
| `createRequest` (ยื่นใบขอลาใหม่) | ✅ | ✅ | ✅ | ต้องล็อกอินแล้ว |
| `viewOwnRequests` (ดูใบลาของตัวเอง) | ✅ | ✅ | ✅ | — |
| `viewAllRequests` (ดูใบลาทุกใบในระบบ) | ❌ | ✅ | ✅ | — |
| `viewRequestDetail` (เปิดดูรายละเอียดใบลา 1 ใบ) | เฉพาะใบของตัวเอง | ✅ ทุกใบ | ✅ ทุกใบ | ผู้ขอลาเปิดใบของคนอื่นไม่ได้ |
| `changeStatus` (เปลี่ยนเป็นอนุมัติ/ไม่อนุมัติ) | ❌ | ✅ | ✅ | ใบต้องมีสถานะ `รอพิจารณา` เท่านั้น เปลี่ยนได้ทางเดียว |
| `addComment` (เขียนความเห็นการอนุมัติ) | เฉพาะใบของตัวเอง | ✅ ทุกใบ | ✅ ทุกใบ | ข้อความห้ามว่าง |
| `deleteRequest` (ลบใบลา) | เฉพาะใบของตัวเอง | ❌ | ❌ | ต้องเป็นเจ้าของใบ **และ** สถานะยังเป็น `รอพิจารณา` เท่านั้น |
| `assignApprover` (กำหนดผู้อนุมัติให้แต่ละใบ) | ❌ | ❌ | ✅ | — |
| `manageLeaveTypes` (เพิ่ม/แก้/ลบ ประเภทการลา) | ❌ | ❌ | ✅ | — |
| `viewDashboard` (ดูแดชบอร์ดสรุปทั้งระบบ) | ❌ | ❌ | ✅ | หน้าโครงจาก prototype ต่อข้อมูลจริงใน Module 3 |

## กติกาการเปลี่ยนสถานะ (หัวข้อ 6 ของสเปก)

```
รอพิจารณา  →  อนุมัติ        (ปลายทาง เปลี่ยนต่อไม่ได้)
           └→  ไม่อนุมัติ    (ปลายทาง เปลี่ยนต่อไม่ได้)
```

- ใบลาใหม่ทุกใบเริ่มต้นที่ `รอพิจารณา` เสมอ (ระบบตั้งให้อัตโนมัติ ผู้ใช้เลือกเองไม่ได้)
- เปลี่ยนสถานะได้เฉพาะผู้อนุมัติ (`manager`) และฝ่ายบุคคล (`hr`) เท่านั้น
- แก้เฉพาะช่อง `status` ห้ามเขียนทับช่องอื่นในไฟล์เดิม
- ใบที่เป็น `อนุมัติ` หรือ `ไม่อนุมัติ` แล้ว เปลี่ยนสถานะต่อไม่ได้อีก

## ผู้เขียนสิทธิ์นี้ตรงกับ

- `js/acl.js` — `ตรวจสิทธิ์(action, ctx)`
- `firestore.rules` — บังคับจริงในระดับฐานข้อมูล (ครอบคลุม `users`, `leaveTypes`, `leaveRequests`, `leaveRequests/{id}/approvals`)
